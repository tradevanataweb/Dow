services:
  - type: web
    name: downloader-api
    runtime: python
    repo: https://github.com/tradevanataweb/Dow
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn server:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
    plan: free
